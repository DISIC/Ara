describe.skip("Audit", () => {
  it("User can create an audit", () => {});
  it("Use can fill an audit (status, description, recommendation, image, impact, easy to fix)", () => {});
  it("User can check Markdown syntax", () => {});
  it("User can complete a11y statement", () => {});
  it("User can update notes", () => {});
  it("User can filter criteria", () => {});
  it("User can search criteria", () => {});
  it("User can copy an audit", () => {});
  it("User can delete an audit", () => {});
  it("User can downlaod an audit", () => {});
});
